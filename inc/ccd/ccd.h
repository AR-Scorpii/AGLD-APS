/**
 * @file    ccd.h
 * 
 * @brief   The compiler compatible definition (CCD) allows a cross-compiler
 *          programming between small device C compiler (SDCC) and Keil C51. 
 *          A view-only scope is provided that allows to read outside of given 
 *          IDEs. 
 */

#ifndef     __CCD_H__
#define     __CCD_H__

#include "regredirect.h"
#include "specfunc.h"

#endif